# Backend Environment (Phase 2)
PORT=3000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pet?schema=public"

# Email / SMTP (Phase 18)
SMTP_HOST="sandbox.smtp.mailtrap.io"
SMTP_PORT=2525
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@pet.local"
